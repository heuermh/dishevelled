
    /** Area node for the ${lower0} ${lower1} view. */
    private final AreaNode ${lower0}${Upper1} = new AreaNode();

    /** Label for the size of the ${lower0} ${lower1} view. */
    private final PText ${lower0}${Upper1}Size = new PText();

        addChild(${lower0}${Upper1});

        addChild(${lower0}${Upper1}Size);

        ${lower0}${Upper1}Size.setText(String.valueOf(getModel().${lower0}${Upper1}().size()));

        offset(${lower0}${Upper1}.getArea(), ${lower0}${Upper1}Size);

